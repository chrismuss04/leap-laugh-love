spring:
  datasource:
    url: jdbc:h2:mem:leaplaughlove;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

server:
  port: 0

app:
  jwt:
    secret: test-only-secret-key-that-is-long-enough-for-hs256-signing
    expiration-minutes: 60
